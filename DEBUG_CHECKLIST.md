# 🔍 Диагностика проблем Render

## Что проверить в Dashboard:

### 1️⃣ **Проверьте логи деплоя:**

**В Dashboard:**
1. Services → (ваш сервис raddscr)
2. Вкладка **"Logs"**
3. Ищите ошибки:

**❌ Типичные ошибки:**

```
ModuleNotFoundError: No module named 'app'
→ Проблема: неправильный startCommand

ERROR: Could not find a version that satisfies...
→ Проблема: зависимости в requirements.txt

sqlalchemy.exc.OperationalError
→ Проблема: база данных не подключена

Connection refused (Redis)
→ Проблема: Redis не создан
```

### 2️⃣ **Проверьте статус сервиса:**

**Должно быть:**
- 🟢 **Live** (зелёный)

**Если:**
- 🟡 **Building** → подождите 2-5 минут
- 🔴 **Deploy failed** → смотрите логи (шаг 1)
- 🟠 **Suspended** → сервис не отвечает >15 минут

### 3️⃣ **Проверьте базы данных:**

**В Dashboard:**
1. Слева: **"PostgreSQL"**
   - Должен быть: `raddscr-db` (Status: Available)

2. Слева: **"Redis"**
   - Должен быть: `raddscr-redis` (Status: Available)

**Если баз нет:**
- Blueprint не применился корректно
- Нужно создать вручную

### 4️⃣ **Проверьте Environment Variables:**

**В сервисе:**
1. Settings → Environment
2. Должны быть установлены:
   ```
   ✅ DATABASE_URL = postgresql://...
   ✅ REDIS_URL = redis://...
   ✅ SECRET_KEY = (любое значение)
   ✅ ALGORITHM = HS256
   ✅ ACCESS_TOKEN_EXPIRE_MINUTES = 43200
   ✅ FRONTEND_URL = http://localhost:3000
   ```

**Если DATABASE_URL или REDIS_URL пустые:**
- База не подключилась
- Нужно переподключить вручную

---

## 🛠️ Быстрые фиксы:

### Фикс 1: Базы созданы, но не подключены

**В сервисе raddscr:**
1. Settings → Environment → Add Environment Variable
2. **DATABASE_URL:**
   - Откройте PostgreSQL `raddscr-db` → Internal Connection String
   - Скопируйте значение (начинается с `postgresql://`)
3. **REDIS_URL:**
   - Откройте Redis `raddscr-redis` → Internal Connection String
   - Скопируйте значение (начинается с `redis://`)
4. Save Changes → Manual Deploy

### Фикс 2: Ошибка в билде (зависимости)

**Если видите ошибки про модули:**

Проверьте логи на:
```
ERROR: Could not find a version
```

**Решение:**
1. Settings → Build Command:
   ```
   cd backend && pip install --upgrade pip && pip install -r requirements.txt
   ```
2. Manual Deploy

### Фикс 3: Неправильный startCommand

**Если видите:**
```
ModuleNotFoundError: No module named 'app'
```

**Решение:**
1. Settings → Start Command:
   ```
   cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT
   ```
2. Manual Deploy

---

## 📋 Что мне прислать для диагностики:

Скопируйте из Dashboard и отправьте:

1. **Последние 20-30 строк логов** (самые свежие)
2. **Статус сервиса** (Live/Building/Failed)
3. **Environment Variables** (список ключей, не значения!)
4. **Есть ли raddscr-db и raddscr-redis?** (Yes/No)

---

## ⚡ Или дайте скриншот:

Просто сделайте скриншот страницы Logs и отправьте мне!
