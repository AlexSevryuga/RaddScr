# 🚀 Render Auto-Deploy Setup

## ✅ Что создано:

1. **render.yaml** — конфигурация для автоматического деплоя
2. Закоммичено и запушено в GitHub

---

## 📋 Что делать дальше:

### Вариант A: Создать новый Blueprint (рекомендуется)

1. Откройте https://dashboard.render.com
2. Нажмите **New** → **Blueprint**
3. Подключите репозиторий: `github.com/AlexSevryuga/RaddScr`
4. Render автоматически:
   - Создаст сервис `raddscr`
   - Создаст PostgreSQL `raddscr-db`
   - Создаст Redis `raddscr-redis`
   - Настроит все environment variables
5. **Apply** → дождитесь завершения (~5 минут)

### Вариант B: Обновить существующий сервис вручную

Если хотите оставить существующий сервис `RaddScr`:

#### 1️⃣ Создать PostgreSQL:
- Dashboard → **New** → **PostgreSQL**
- Name: `raddscr-db`
- Region: Oregon
- Plan: **Free**
- Create Database

#### 2️⃣ Создать Redis:
- Dashboard → **New** → **Redis**
- Name: `raddscr-redis`
- Region: Oregon
- Plan: **Free** (25MB)
- Create Redis

#### 3️⃣ Обновить Environment Variables в сервисе `RaddScr`:
- Откройте сервис → **Environment**
- Добавьте:
  ```
  DATABASE_URL = [скопируйте Internal Database URL из PostgreSQL]
  REDIS_URL = [скопируйте Redis URL]
  SECRET_KEY = [любая строка, например: supersecretkey123]
  ALGORITHM = HS256
  ACCESS_TOKEN_EXPIRE_MINUTES = 43200
  FRONTEND_URL = http://localhost:3000
  PYTHON_VERSION = 3.11.0
  ```

#### 4️⃣ Обновить Build & Deploy Settings:
- **Build Command:** `cd backend && pip install -r requirements.txt`
- **Start Command:** `cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT`

#### 5️⃣ Сохранить и дождаться редеплоя

---

## 🎯 После успешного деплоя:

### Проверка:
```bash
curl https://raddscr.onrender.com/health
# Должен вернуть: {"status":"healthy"}
```

### API Docs:
```
https://raddscr.onrender.com/docs
```

### Следующие шаги:
1. ✅ Инициализировать БД: `POST /init-db` (нужно создать endpoint)
2. ✅ Зарегистрировать тестового пользователя
3. ✅ Создать проект для валидации
4. ✅ Проверить Celery worker (если включен)

---

## 🔧 Troubleshooting:

### Сервис не запускается:
- Проверьте логи в Render Dashboard
- Убедитесь, что `DATABASE_URL` и `REDIS_URL` установлены
- Проверьте, что сборка прошла успешно

### "Module not found" ошибки:
- Убедитесь, что Build Command указывает на `backend/requirements.txt`
- Проверьте, что Start Command выполняется из папки `backend/`

### Database connection failed:
- Проверьте, что PostgreSQL создан и доступен
- Убедитесь, что `DATABASE_URL` содержит правильный Internal URL

---

## 📊 Текущий статус:

- ✅ Код запушен в GitHub
- ✅ render.yaml создан
- ⏳ Ожидается деплой через Blueprint или ручная настройка

**Выберите Вариант A (Blueprint) для полностью автоматического деплоя!**
